const blue = '#334FFA';
const purpleLight = '#CFD6FF';
const snow = '#FFFFFF';
const smoke = '#F8F8F8';
const dark = '#000000';
const steel = '#9B9898';
const lava = '#00B833';
const forest = '#FF0000';

const theme = {
  colors: {
    primaryColor: blue,
    primaryLight: purpleLight, 
    background: smoke,
    white: snow,
    danger: lava,
    success: forest,
    dark,
    primaryText: dark,
    secondaryText: steel,
  },
};

export {
  theme
};